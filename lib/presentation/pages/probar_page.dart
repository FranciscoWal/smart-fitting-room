import 'package:flutter/material.dart';

class ProbarPage extends StatelessWidget {
  const ProbarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Vista de prueba AR/VR', style: TextStyle(fontSize: 18)),
    );
  }
}
